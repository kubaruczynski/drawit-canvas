{
  "name": "drawit-canvas",
  "version": "1.1.1",
  "source": "src/index.html",
  "license": "MIT",
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-typescript"
    ]
  },
  "scripts": {
    "start": "parcel --open",
    "build": "parcel build --public-url /drawit-canvas/ --dist-dir docs",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.0.0",
    "parcel": "^2.0.0"
  },
  "jest": {
    "verbose": true,
    "moduleDirectories": [
      "node_modules",
      "src"
    ]
  }
}
